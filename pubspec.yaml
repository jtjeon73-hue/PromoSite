name: flutter_promo_site

description: 소통 플랫폼 — 소통사주·소통건강·소통팜지기·소통사매 Flutter Web 랜딩페이지
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
